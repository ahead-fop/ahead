import javax.swing.*;

refines class optprim {
    //draw an optional primitive
    public JComponent draw (int several) {
        if (var.hidden)
           return null;
        else{
           JCheckBox t = new JCheckBox(var.disp);
           var.userVisible = true;  // *dsb*
           t.setToolTipText("Optional");
           return t;
           }
    }
}
