// This program tests that calling the same macro twice in the same
// program works correctly.

public class test4 {
    #SORT(id{ fooSort }id, fooType)

    #SORT(id{ another_fooSort }id, fooType2)

    static public void main(String[] args) {
	int[] iarray = new int[5];
	fooSort(iarray);
    }
}
