layer FS;

import fsats.util.DataContainer;

import laydown.*;

import java.util.*;

SoUrCe RooT MessageL "C:!Java!FeatureOrientedProgramming!fsats!MessageL!LockGranted.jak";

/** Notifies a mission that its lock on an opfac has been granted. */
public class LockGranted
        extends BasicEvent {
    public EventType getType() {
        return EventType.lockGranted;
    }
    public LockGranted( String missionId ) { Super( String )( missionId );
    }
}
